require 'spec_helper'

describe SimpleOpenWeatherMap do
  it 'has a version number' do
    expect(SimpleOpenWeatherMap::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
